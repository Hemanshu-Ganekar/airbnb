
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title>Add Home to Airbnb</title>
  </head>
  <body class="bg-gray-50 font-sans">
    <header class="shadow-md bg-red-400 text-white">
      <h1 class="text-white text-3xl md:text-4xl font-semibold p-6 text-center">
        Register Your Home
      </h1>
    </header>

    <main class="mt-10 flex justify-center">
      <div class="bg-white shadow-lg rounded-2xl p-8 w-full max-w-lg">
          <form action="<%=edit=='false'?'/host/add-home':'/host/editHome'%>" method="POST" class="space-y-6">
            <% if(edit=="true"){%>
               <input type="hidden" name="id" value="<%= home._id%>">
              <%}%>
          <!-- Label -->
          <label for="houseName" class="block text-left text-gray-700 text-lg font-medium">
            House Name
          </label>
          <!-- Input -->
          <input
            id="houseName"
            class="border border-gray-300 w-full rounded-xl p-3 focus:outline-none focus:ring-2 focus:ring-red-500 shadow-sm"
            type="text"
            name="houseName"
            placeholder="e.g., Cozy Beachfront Cottage"
            value="<%= edit == 'true' ? home.houseName : '' %>"
          />
          <!-- Label -->
          <label for="pricePerNight" class="block text-left text-gray-700 text-lg font-medium">
            Price Per Night
          </label>
          <!-- Input -->
          <input
            id="pricePerNight"
            class="border border-gray-300 w-full rounded-xl p-3 focus:outline-none focus:ring-2 focus:ring-red-500 shadow-sm"
            type="text"
            name="pricePerNight"
            placeholder="e.g., 5000 per Night"
            value="<%= edit == 'true' ? home.pricePerNight : '' %>"
          />
          <!-- Label -->
          <label for="location" class="block text-left text-gray-700 text-lg font-medium">
            Location
          </label>
          <!-- Input -->
          <input
            id="location"
            class="border border-gray-300 w-full rounded-xl p-3 focus:outline-none focus:ring-2 focus:ring-red-500 shadow-sm"
            type="text"
            name="location"
            placeholder="e.g., Panji,Goa "
            value="<%= edit == 'true' ? home.location : '' %>"
          />
              <!-- Label -->
          <label for="Rating" class="block text-left text-gray-700 text-lg font-medium">
            Rating
          </label>
          <!-- Input -->
          <input
            id="Rating"
            class="border border-gray-300 w-full rounded-xl p-3 focus:outline-none focus:ring-2 focus:ring-red-500 shadow-sm"
            type="text"
            name="Rating"
            placeholder="e.g. 4.5 out of 5 "
            value="<%= edit == 'true' ? home.Rating : '' %>"
          />
                   <!-- Label -->
          <label for="Photo" class="block text-left text-gray-700 text-lg font-medium">
            Photo
          </label>
          <!-- Input -->
          <input
            id="Photo"
            class="border border-gray-300 w-full rounded-xl p-3 focus:outline-none focus:ring-2 focus:ring-red-500 shadow-sm"
            type="text"
            name="Photo"
            placeholder="URL"
            value="<%= edit == 'true' ? home.Photo : '' %>"
          />
                             <!-- Label -->
          <label for="description" class="block text-left text-gray-700 text-lg font-medium">
            Description
          </label>
          <!-- Input -->
          <textarea
            id="description"
            class="border border-gray-300 w-full rounded-xl p-3 focus:outline-none focus:ring-2 focus:ring-red-500 shadow-sm"
            type="text"
            name="description"
            placeholder="Description of your property"
          ><%= edit == 'true' ? home.description : '' %></textarea>
          <!-- Submit Button -->
          <button
            type="submit"
            class="w-full bg-red-400 hover:bg-red-600 text-white font-semibold rounded-xl py-3 transition duration-200 shadow-md"
          >
            <%=edit=='true'?'Save Edit':'Add Home'%>
          </button>
        </form>
      </div>
    </main>
  </body>
</html>
